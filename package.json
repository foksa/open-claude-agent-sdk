{
  "name": "@lite-claude/agent-sdk",
  "version": "0.0.0",
  "description": "A lightweight alternative to Claude Agent SDK - 70x smaller, uses local CLI",
  "license": "MIT",
  "author": "lite-claude contributors",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/foksa/lite-claude-agent-sdk.git"
  },
  "bugs": {
    "url": "https://github.com/foksa/lite-claude-agent-sdk/issues"
  },
  "homepage": "https://github.com/foksa/lite-claude-agent-sdk#readme",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target node --format esm && bun run build:types",
    "build:types": "tsc -p tsconfig.build.json",
    "test": "bun test",
    "test:unit": "bun test tests/unit/",
    "test:integration": "bun test tests/integration/",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "format": "biome format .",
    "format:write": "biome format --write .",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "ci": "bun run typecheck && bun run check && bun run test && bun run build",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "bun run build && changeset publish"
  },
  "keywords": [
    "ai",
    "agent",
    "claude",
    "anthropic",
    "sdk",
    "bun"
  ],
  "engines": {
    "bun": ">=1.3.0"
  },
  "peerDependencies": {
    "@anthropic-ai/sdk": "^0.30.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.72.1",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.34",
    "@biomejs/biome": "^2.3.13",
    "@changesets/cli": "^2.29.8",
    "@types/bun": "^1.3.8",
    "playwright": "^1.58.1",
    "typescript": "^5.9.3"
  }
}
